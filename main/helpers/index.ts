import createWindow from './create-window';
import exitOnChange from './exit-on-change';
import backupVersion from './backup-version';
import updateVersion from './updade-version';
import restoreVersion from './restore-version';

export {
  createWindow,
  exitOnChange,
  backupVersion,
  updateVersion,
  restoreVersion,
};
